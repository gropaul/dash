# name: test/sql/duck_explorer.test
# description: test duck_explorer extension
# group: [duck_explorer]

# Before we load the extension, this will fail
statement error
SELECT duck_explorer('Sam');
----
Catalog Error: Scalar Function with name duck_explorer does not exist!

# Require statement will ensure this test is run with this extension loaded
require duck_explorer

# Confirm the extension works
query I
SELECT duck_explorer('Sam');
----
DuckExplorer Sam 🐥

query I
SELECT duck_explorer_openssl_version('Michael') ILIKE 'DuckExplorer Michael, my linked OpenSSL version is OpenSSL%';
----
true
